extends base

block content
    div 
        p=video.description
            ul 
                each hashtag in video.hashtags
                    li=hashtag
        small=video.createAt
    a(href=`${video.id}/edit`) Edit Video 